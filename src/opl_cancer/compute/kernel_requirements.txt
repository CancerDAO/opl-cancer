# bixbench kernel requirements — conda + pip mixed packages installed into the
# image at `compute/bixbench.Dockerfile` build time. The Dockerfile does:
#   COPY kernel_requirements.txt .
#   RUN mamba install -c conda-forge --file kernel_requirements.txt -y
# so every line must be a valid conda package spec (one per line, conda-forge
# resolvable). Pin minor versions to keep image reproducible.
#
# v1.5: created to unblock Docker build (file was referenced but missing in
# v1.4 — see docs/ANTI_PATTERNS_v1.4.md AP-14).
# Scope: the minimum Python bioinformatics stack the Wave-3 recipes need.
# For native (non-Docker) execution see compute/native_runner.py.
pandas=2.2.2
numpy=1.26.4
scipy=1.13.1
statsmodels=0.14.2
scikit-learn=1.4.2
matplotlib=3.8.4
seaborn=0.13.2
plotly=5.22.0
networkx=3.3
pyyaml=6.0.1
requests=2.32.3
httpx=0.27.0
lifelines=0.27.8
scanpy=1.10.1
anndata=0.10.7
